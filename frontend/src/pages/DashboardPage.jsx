import { useEffect, useState } from 'react';import Navbar from '../components/Navbar';import API from '../services/api';export default function DashboardPage() { const [stats, setStats] = useState({ total: 0, done: 0, pending: 0, overdue: 0 }); const [loading, setLoading] = useState(true); useEffect(() => { const fetchStats = async () => { try { const response = await API.get('/tasks'); const tasks = response.data.tasks; const now = new Date(); setStats({ total: tasks.length, done: tasks.filter(t => t.status === 'done').length, pending: tasks.filter(t => t.status !== 'done').length, overdue: tasks.filter(t => t.status !== 'done' && new Date(t.deadline) < now).length }); } finally { setLoading(false); } }; fetchStats(); }, []); return <div><Navbar /><div className="max-w-7xl mx-auto p-8"><h1 className="text-4xl font-bold mb-8">Dashboard</h1>{loading ? <div>Loading...</div> : <div className="grid grid-cols-1 md:grid-cols-4 gap-4"><div className="bg-blue-500 text-white p-6 rounded-lg shadow"><div className="text-4xl font-bold">{stats.total}</div><div>Total Tasks</div></div><div className="bg-green-500 text-white p-6 rounded-lg shadow"><div className="text-4xl font-bold">{stats.done}</div><div>Completed</div></div><div className="bg-yellow-500 text-white p-6 rounded-lg shadow"><div className="text-4xl font-bold">{stats.pending}</div><div>Pending</div></div><div className="bg-red-500 text-white p-6 rounded-lg shadow"><div className="text-4xl font-bold">{stats.overdue}</div><div>Overdue</div></div></div>}</div></div>; }