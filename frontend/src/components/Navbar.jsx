import { useNavigate } from 'react-router-dom';import { useAuth } from '../context/AuthContext';export default function Navbar() { const navigate = useNavigate(); const { user, logout } = useAuth(); const handleLogout = () => { logout(); navigate('/login'); }; return <nav className="bg-blue-600 text-white p-4 shadow-lg"><div className="max-w-7xl mx-auto flex justify-between items-center"><div className="text-2xl font-bold">📋 Task Manager</div><div className="flex gap-6 items-center"><a href="/dashboard" className="hover:text-blue-200">Dashboard</a><a href="/projects" className="hover:text-blue-200">Projects</a><a href="/tasks" className="hover:text-blue-200">Tasks</a><span className="text-sm">{user?.email}</span><button onClick={handleLogout} className="bg-red-500 hover:bg-red-600 px-4 py-2 rounded">Logout</button></div></div></nav>; }